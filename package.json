{
  "name": "notion-plan-publisher",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "bin": { "plan-publish": "dist/cli.js" },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "lint": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@notionhq/client": "^2.2.15",
    "js-yaml": "^4.1.0",
    "dotenv": "^16.4.7"
  },
  "devDependencies": {
    "@types/js-yaml": "^4.0.9",
    "@types/node": "^22.10.2",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
